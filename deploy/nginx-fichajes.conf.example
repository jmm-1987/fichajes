# Ejemplo para Nginx en Ubuntu 24.04.
# Copiar a /etc/nginx/sites-available/fichajes y enlazar en sites-enabled.
# Sustituir fichajes.ejemplo.com por su dominio.

upstream fichajes_gunicorn {
    server 127.0.0.1:8000 fail_timeout=0;
}

server {
    listen 80;
    listen [::]:80;
    server_name fichajes.ejemplo.com;

    # Tras certbot, descomentar la redirección a HTTPS:
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://fichajes_gunicorn;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        proxy_read_timeout 120s;
    }

    client_max_body_size 16M;
}

# Bloque sugerido tras: sudo certbot --nginx -d fichajes.ejemplo.com
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name fichajes.ejemplo.com;
#     ssl_certificate /etc/letsencrypt/live/fichajes.ejemplo.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/fichajes.ejemplo.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://fichajes_gunicorn;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_redirect off;
#         proxy_read_timeout 120s;
#     }
#
#     client_max_body_size 16M;
# }
